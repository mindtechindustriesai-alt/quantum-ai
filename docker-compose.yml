version: '3.8'
services:
  backend:
    image: python:3.11-slim
    working_dir: /app
    volumes:
      - .:/app
    command: bash -c "pip install -r backend/requirements.txt && cd backend && uvicorn main:app --host 0.0.0.0 --port 8000"
    ports:
      - "8000:8000"
